// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>
int main()
{
    float p,r,t,a,si,ci;
    printf(" enter the temp in ci");
    scanf("%f%f%f" , &p,&r,&t);
    si=(p*r*t)/100;
    printf("simple intrest=%f" ,si);
    a=p*(((1+r/100),t));
    ci=a-p;
    printf("compound intrest=%f" ,ci);


    return 0;
}
