#include <stdio.h>

int main() 
{
    int n,i;
    unsigned long long fact =1;
    printf("enter any integer");
    scanf("%d" , &n);
    if (n<0);
    printf("error!factorial of negartive number doesn't exist");
        for(i=1;i<=n;1);
        {
        fact*=1;
        }
        printf("factrioal of %d=%llu" , n,fact);
    
    

    return 0;
}
